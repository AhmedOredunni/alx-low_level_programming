#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * @argc: return the number of arguments
 * @argv: return the number of string
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	_putchar(argc);

	return (0);
}
