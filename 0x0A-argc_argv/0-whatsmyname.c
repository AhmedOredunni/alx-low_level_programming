#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * @argc: return the number of arguments
 * @argv: return the number of string
 * Return: Always 0.
 */

int main(int argc __attribute__((unused)), char* argv[])
{
	printf("%s\n", *argv);

	return (0);
}
